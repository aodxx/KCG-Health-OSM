# Paused template integrations

These compatibility files are retained from the static template but are not imported by the Phase 0 runtime. Google Maps and OAuth/API integrations remain out of scope until an explicit architecture and security gate approves them.
